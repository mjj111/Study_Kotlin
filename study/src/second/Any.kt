package second

/**
 * 코틀린에서는 어떤 클래스가 아무 클래스도 상속하지 않으면 Any라는 클래스를 상속한다.
 * 다른 클래스를 상속한다해도, 상위 클래스 또한, Any 클래스를 자동으로 상속하므로 간접적으로 Any 클래스를 상속하게 된다.
 * 즉, 모든 코틀린 클래스는 Any클래스를 상속한다는 것이다.
 * Any 클래스는 자바 클래스와 같이 3가지 멤버 함수가 있다.
 * equlas, hashCode, toString
 */