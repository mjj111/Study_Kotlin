package fourth

/**
 * sorted 계열의 확장 함수를 이용하여 컬렉션 내의 원소를 정렬한다.
 * sorted 함수는 Iterable과 Array에 선언되어 오름차순으로 정렬된 List<T>값을 환한다.
 *
 * 이떄, 반드시 각 원소는 Comparable 인터페이스를 구현하고 있어야한다.
 * 반대 방향인 sortedDescending도 있다.
 *
 */